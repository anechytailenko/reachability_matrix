from Visualization import *
from Graph import *
from GraphGenerator import *
from WarshallAlgorithm import *

graph = GraphGenerator.generate(7, 10)

# reachability_matrix = WarshallAlgorithm.get_reachability_matrix(graph)

# Visualization.visualize_matrix(reachability_matrix, labels=False)

# Visualization.visualize_graph(graph, labels=True)
